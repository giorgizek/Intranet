@{
    var controller = ViewContext.GetController();
    var action = ViewContext.GetAction();
}
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a asp-area="" asp-controller="Home" asp-action="Index" class="navbar-brand">Intranet</a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="@(controller == "Home" && action == "Index" ? "active" : "")"><a asp-area="" asp-controller="Home" asp-action="Index"><i class="fa fa-home" aria-hidden="true"></i> Home</a></li>
                @*<li class="@(controller == "Home" && action == "About" ? "active" : "")"><a asp-area="" asp-controller="Home" asp-action="About"><i class="fa fa-info" aria-hidden="true"></i> About</a></li>
                <li class="@(controller == "Home" && action == "Contact" ? "active" : "")"><a asp-area="" asp-controller="Home" asp-action="Contact"><i class="fa fa-phone" aria-hidden="true"></i> Contact</a></li>*@
            </ul>
            @await Html.PartialAsync("_LoginPartial")
        </div>
    </div>
</nav>